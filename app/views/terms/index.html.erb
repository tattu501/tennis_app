<h1 class="title">用語集</h1>
<% if logged_in? && admin_user? %>
  <%= link_to "登録", new_term_path, class: "btn btn-lg btn-primary
    btn-indent-top-second btn-size btn-fixed-insert" %>
<% end %>
<div class="term-index">
  <ul>
    <% @terms.each do |term| %>
      <li><%= link_to  term.term, term, class: 'btn btn-primary btn-experiences' %></li>
    <% end %>
  </ul>
</div>