<div class="experience_wrapper">
  <% breadcrumb :experience_new %>
  <%= render "shared/breadcrumbs" %>
  <div class="experience_wrapper__header">
    <h3>体験記の登録</h3>
  </div>
  <div class="experience_wrapper__content">
    <%= form_with(model: @experience, local: true) do |f| %>
      <%= render 'shared/experience_messages' %>

      <%= f.label :date, "date(年月日)" %>
      <%= f.date_field :date, use_month_numbers: true, start_year: 2019, class: "form-control date-picker" %>

      <%= f.label :explanation, "explanation(内容)" %>
      <%= f.text_area :explanation, class: 'form-control' %>

      <%= f.submit "登録確定", class: "btn btn-primary" %>
      <span class="image">
        <%= f.file_field :image %>
      </span>
    <% end %>
  </div>
</div>